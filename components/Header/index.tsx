import { SHeader, SHeaderContainer } from './Header.styled';

const Header = () => {
  return (
    <SHeader>
      <SHeaderContainer>
        <h1>Header</h1>
      </SHeaderContainer>
    </SHeader>
  );
};

export default Header;
